# Zenmo API base URL (default in code: http://localhost:8080 when unset).
# Use `relative` for a production build served behind Caddy (Docker image) that proxies /api to the backend.
VITE_API_URL=http://localhost:8080
