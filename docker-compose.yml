services:
  zenmo-ui:
    build:
      context: .
      args:
        # Set to your production API URL before deploy
        VITE_API_URL: ${VITE_API_URL:-http://localhost:8080}
    ports:
      - "${HTTP_PORT:-8080}:80"
    restart: unless-stopped
