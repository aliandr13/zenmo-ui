# Production: mount over /etc/caddy/Caddyfile (e.g. docker compose volumes).
# Requires DNS for zenmo.fyi → this host and open ports 80 + 443.
# Set CADDY_ACME_EMAIL in the container env for Let's Encrypt account contact (recommended).
{
	email {$CADDY_ACME_EMAIL}
}

zenmo.fyi {
	import zenmo-app.caddy
}
